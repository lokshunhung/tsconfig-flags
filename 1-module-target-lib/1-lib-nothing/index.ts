// Note: The "// @ts-expect-error" is a magic comment that
// tells TS compiler the next line should contain an error.
// If there is no error, TS compiler throws an error for this.

// @ts-expect-error
console.log();

// @ts-expect-error
let one = String(1);
